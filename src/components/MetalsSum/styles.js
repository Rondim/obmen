export default (theme) => {
  return {
    paper: {
      height: '100%'
    },
    totalWeight: {
      padding: '0 10px',
      position: 'relative',
      top: '50%',
      transform: 'perspective(1px) translateY(-50%)'
    }
  };
};
